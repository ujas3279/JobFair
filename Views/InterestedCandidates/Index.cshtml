@model IEnumerable<JobFair.Models.InterestedCandidate>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Company)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Seeker)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Company.ApplyLink)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Seeker.HighestQualification)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Icid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Icid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Icid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
